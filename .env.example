# PUBLIC_SHARE_API — base URL for share worker (no trailing slash)
# Local dev with worker: http://127.0.0.1:8787/api
# Production: https://your-worker.workers.dev/api or /api if routed via Pages
PUBLIC_SHARE_API=http://127.0.0.1:8787/api
